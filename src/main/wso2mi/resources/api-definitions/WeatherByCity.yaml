openapi: 3.0.1
info:
  title: WeatherByCity
  description: API Definition of WeatherByCity
  version: 1.0.0
servers:
- url: http://localhost:8290/weather
- url: https://localhost:8253/weather
paths:
  /:
    get:
      parameters:
      - name: city
        in: query
        required: false
        style: form
        explode: true
        schema:
          type: string
      responses:
        default:
          description: Default response
